[email_settings]
from = "Backup Runner <{{ email.username }}>"
to = "Ville Kalliomäki <{{ contact_email }}>"
host = "{{ email.host }}"
port = {{ email.port }}
username = "{{ email.username }}"
password = "{{ email.password }}"

{% for backup in backups %}
[[backups]]
name = "{{ backup.name }}"
source_paths = "{{ backup.source_paths }}"
endpoint = "{{ backup.endpoint }}"
repo_path = "{{ backup.repo_path }}"
user = "{{ backup.user }}"
ssh_key_path = "{{ backup.ssh_key_path }}"
repo_password = "{{ backup.repo_password }}"
{% if backup.no_history is defined %}
no_history = "{{ backup.no_history }}"
{% endif %}
{% endfor %}
