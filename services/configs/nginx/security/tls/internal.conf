include /etc/nginx/security/tls/general.conf;

# Key, cert and dhparams for internal TLS
ssl_certificate /etc/certificates/internal.crt;
ssl_certificate_key /etc/certificates/internal.key;
ssl_dhparam /etc/certificates/internal_dhparams.pem;
