---
# defaults file for redis

redis_container_image: docker.io/library/redis:7.2

redis_recreate: false

# By default nothing is mounted, but this array is populated automatically when a data path is provided
redis_volumes_internal: []
